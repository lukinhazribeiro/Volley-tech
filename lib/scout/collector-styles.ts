// Estilos compartilhados dos botões do coletor (faces).
// Centralizados aqui para manter os toques grandes, consistentes e profissionais
// em todas as faces, evitando cliques perdidos durante a coleta ao vivo.

// Grade de números (seleção de jogador): alvos grandes e quadrados.
export const numberGrid = "grid grid-cols-5 gap-2.5"

// Botão de número (camisa do atleta).
export const numberBtn =
  "h-16 text-2xl font-bold rounded-xl shadow-sm transition-transform duration-100 active:scale-95"

// Grade de opções com 2 colunas.
export const choiceGrid2 = "grid grid-cols-2 gap-3"

// Grade de opções com 3 colunas.
export const choiceGrid3 = "grid grid-cols-3 gap-3"

// Botão de opção (rótulos como Ponto, Erro, Ponta, Meio...).
export const choiceBtn =
  "h-16 text-lg font-semibold rounded-xl shadow-sm transition-transform duration-100 active:scale-95"

// Botão de opção em largura total (ações destacadas).
export const choiceBtnFull =
  "h-16 w-full text-lg font-semibold rounded-xl shadow-sm transition-transform duration-100 active:scale-95"

// Título da face.
export const faceTitle = "text-xl font-bold text-slate-900 mb-1"

// Subtítulo/legenda da face.
export const faceSubtitle = "text-sm text-slate-500 mb-4"
